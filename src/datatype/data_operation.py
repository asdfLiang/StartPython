"""
    数据运算
"""

n = 3
m = 5

# 基本运算，加减乘除、平方、整除、取余
print("n + m = ", n + m)
print("n - m = ", n - m)
print(f"n * m = {n * m}")
print(f"m / n = {m / n}")

print("n**m =", n**m)
print("m // n =", m // n)
print("m % n =", m % n)

# 逻辑运算, and、or、not

# 比较运算，等于(==)、不等于(!=)、大于(>)、小于(<)、大于等于(>=)、小于等于(<=)

# 位运算，按位与(&)、按位或(|)、按位异或(^)、按位取反(~)、左位移(<<)、右位移(>>)

# 进制转换
